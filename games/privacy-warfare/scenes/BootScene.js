// scenes/BootScene.js — Boot/loading scene (stub; game boots directly)
// The overlay DOM element in index.html serves as the menu.
export class BootScene {
  onEnter() {}
  update() {}
  render() {}
}
